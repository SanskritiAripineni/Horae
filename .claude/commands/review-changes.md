Review all uncommitted changes in the repository.

Steps:
1. Run `git status` to see modified/untracked files
2. Run `git diff` to see the actual changes
3. For each changed file, provide a brief summary of what changed and why it looks correct or concerning
4. Flag any potential issues: secrets accidentally staged, large binaries, broken imports, etc.
5. Suggest a commit message if the changes look ready
