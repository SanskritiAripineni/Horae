package com.google.mediapipe.examples.llminference.ui.theme

import androidx.compose.ui.graphics.Color

// ── Legacy (unused, kept for safety) ──────────────────────────
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// ── RIDE Dark Palette ──────────────────────────────────────────
val DarkBackground  = Color(0xFF0D0D0D)
val DarkSurface     = Color(0xFF1A1A1A)
val DarkSurfaceVar  = Color(0xFF222222)
val DarkBorder      = Color(0xFF2A2A2A)
val DarkOnSurface   = Color(0xFFE0E0E0)
val DarkOnSurfaceDim = Color(0xFF888888)

// ── Terminal Accent Colors ─────────────────────────────────────
val TerminalGreen  = Color(0xFF00FF88)   // active / OK
val TerminalAmber  = Color(0xFFFFB300)   // warning / AutoLife
val TerminalCyan   = Color(0xFF00BCD4)   // info / VectorDB
val TerminalRed    = Color(0xFFFF4444)   // error / K-Emo
val TerminalBlue   = Color(0xFF4285F4)   // agent / work events

// ── Tool Card Left-Border Accents ─────────────────────────────
val ToolAutoLife  = Color(0xFFFFB300)   // amber  — AutoLife
val ToolKEmo      = Color(0xFFFF4444)   // red    — K-Emo Phone
val ToolVectorDB  = Color(0xFF00BCD4)   // cyan   — VectorDB
val ToolCalendar  = Color(0xFF00FF88)   // green  — Calendar API
val AgentBlue     = Color(0xFF1A6FD4)   // deep blue — agent header

// ── Schedule Event Bar Colors ──────────────────────────────────
val ScheduleWork    = Color(0xFF4285F4)
val ScheduleHealth  = Color(0xFF34A853)
val ScheduleLeisure = Color(0xFFFBBC04)

// ── PHQ Risk Level Colors ──────────────────────────────────────
val RiskLow      = Color(0xFF00FF88)
val RiskMild     = Color(0xFFFFB300)
val RiskModerate = Color(0xFFFF8C00)
val RiskSevere   = Color(0xFFFF4444)
